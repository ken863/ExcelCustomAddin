# Docker Environment Configuration
# Copy file này thành .env và cập nhật các giá trị theo môi trường của bạn

# Docker Hub Configuration
DOCKER_USERNAME=your-dockerhub-username
VERSION=latest

# Proxy Configuration (optional)
HTTP_PROXY=http://user:pass@proxy.company.com:8080
HTTPS_PROXY=http://user:pass@proxy.company.com:8080
http_proxy=http://user:pass@proxy.company.com:8080
https_proxy=http://user:pass@proxy.company.com:8080

# Certificate Configuration (optional)
# Uncomment nếu mount certificate file
# PROXY_CERT_PATH=/tmp/proxy-cert.crt
